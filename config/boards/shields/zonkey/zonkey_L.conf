# --- NFC / EC11 ---
CONFIG_NFCT_PINS_AS_GPIOS=y

# EC11（エンコーダー）は使用する場合のみ有効
CONFIG_EC11=y
CONFIG_EC11_TRIGGER_GLOBAL_THREAD=y

# --- RGB / バッテリー表示 ---
CONFIG_ZMK_LEDS=n
CONFIG_ZMK_RGB=n
CONFIG_RGBLED_WIDGET_BATTERY_LEVEL_HIGH=30
CONFIG_RGBLED_WIDGET_BATTERY_LEVEL_CRITICAL=10
CONFIG_RGBLED_WIDGET_SHOW_LAYER_COLORS=n  # 消費削減のため無効化

# --- バッテリー / BLE ---
CONFIG_ZMK_BATTERY_REPORTING=y
CONFIG_BT_BAS=y

# --- BLE PHY 省電力化 ---
CONFIG_BT_CTLR_PHY_2M=n    # 高速 PHY は消費増 → 1M PHY に固定

# --- スタックサイズ（必要最低限維持） ---
CONFIG_ZMK_BLE_THREAD_STACK_SIZE=2048
CONFIG_ZMK_LOW_PRIORITY_THREAD_STACK_SIZE=2048
CONFIG_INPUT_THREAD_STACK_SIZE=2048
CONFIG_ZMK_SPLIT_BLE_PERIPHERAL_STACK_SIZE=2048
CONFIG_ZMK_SPLIT_BLE_CENTRAL_SPLIT_RUN_STACK_SIZE=3096
